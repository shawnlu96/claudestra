# 不带结尾斜杠：worktree 里 node_modules 是软链（不是目录），带斜杠的规则匹配不到它
node_modules
.env
.DS_Store
.idea/
master/CLAUDE.md
master/.claude/
.playwright-mcp/
# deploy/relay/deploy.sh 每次部署写入的短 sha，只随 rsync 走
.relay-commit
